//const Book = require('./models/Book');

// app.post('/api/books', (req, res) => {
//   // delete req.body._id;
//   const book = new Book({ ...req.body });
//   book
//     .save()
//     .then(() => res.status(201).json({ message: 'New book register' }))
//     .catch((error) => res.status(400).json({ error }));
//   //res.json({ message: 'Hello World !' });
// });
